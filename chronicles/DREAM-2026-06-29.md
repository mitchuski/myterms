# Dream — myterms bundle — 2026-06-29

## What this is (2-4 lines, naming all 5 dirs)
The **myterms bundle** is a coordinated agentprivacy release: one docs repo and two
MV3 browser extensions (each present twice). `C:\Users\mitch\myterms` is the
agreement-layer documentation + the dual-agent harness (IEEE 7012 / MyTerms Alliance kit).
`mymage` ⊥ `myswordsman` are the *renamed* design+source copies of the two extensions
(🧙 proposer ⊥ ⚔️ prover), now their own git repos. `mages-spell` ⊥ `swordsman-blade`
are the *original published* extension repos those copies were forked from. The whole
bundle echoes into the City of Mages (`MYTERMS_UNIVERSE_CARD.md`) and the guide federation.

## Current state & open threads (per dir)

### `C:\Users\mitch\myterms` — the agreement layer (docs + harness)
- **Purpose:** canonical, externally-shareable MyTerms Alliance founding-member application
  package for IEEE Std 7012-2025 ("agreement layer, not enforcement"). Public repo
  `github.com/mitchuski/myterms`.
- **State:** mature docs. Core: `00_executive_brief.md`, `00_myterms_alliance_application.md`,
  letter docs `A_`–`G_`, `ieee7012_integration_plan_v2.md` (current, Apr 22), the IEEE PDF
  `7012-2025 (3).pdf`. `MYTERMS_UNIVERSE_CARD.md` (06-11) situates it in the City.
  `harness/` (moved in 06-11) = the dual-agent autoresearch loop (`run.mjs`,
  `harness.config.mjs`, `runtime-extension.mjs`, `core/`, LICENSE); retargeted to
  `../../myswordsman` + `../../mymage`, mock re-verified 8 rounds.
- **Open threads:**
  - **Integration plan v3 (V6 edition) is *queued*** — v2 is still operative; v2 cites PVM
    **V5.4** while canon is now **V6** (`R(t)=(C_S(t)+C_M(t))/H(X)`, register `…V6.md` head C89).
  - **PDFs are stale** — letter-doc `.pdf` renders reflect the **Feb 5, 2026** snapshot;
    regeneration against the Apr 22 markdown is pending.
  - README still says siblings are "local, not yet public" — **out of date** (mymage/mysword
    now have public remotes; see naming tangle below).

### `C:\Users\mitch\mymage` — 🧙 Mage extension (renamed copy)
- **Purpose:** the Proposer / knowledge agent; SCAN + INSCRIBE; operates inside the Σ-scope
  the Swordsman signs. Carries the spellbook; proposes, never enforces.
- **State:** `src/` has `background/`, `content/` (cursor-overlay), `lib/` (spell-definitions,
  effect-presets, repertoire-sync, …). **`src/popup/` is EMPTY** and
  `src/shared/grimoire-data/` is EMPTY. `build.js`, `manifest.json`, grimoire JSON bundles
  (v1.2.0, v10.0.0, v10.2.0) present. Now its **own git repo** — remote
  `github.com/mitchuski/mymage.git`, single "Initial commit".

### `C:\Users\mitch\myswordsman` — ⚔️ Swordsman extension (renamed copy)
- **Purpose:** the Prover / IEEE-7012 Individual's Agent (Recorder/Auditor); SLASH + WARD;
  holds `checkCanForge`, constellation-hash witness, bilateral chronicle; renders the canvas;
  runs in a separate Chrome process. Reference: `64_blades_reference_sheet_2426.md`,
  `swordsman-extension-myterms-design.md`, `AGENT_BUILD_INSTRUCTIONS_SWORDSMAN.md`.
- **State:** `src/` + `styles/` + `icons/`; `build.js`, `manifest.json`. Own git repo —
  remote `github.com/mitchuski/mysword.git`, single "Initial commit".

### `C:\Users\mitch\mages-spell` — 🧙 original published Mage extension
- **State:** the **source of the rename**. `dist/` build exists but **stale (Mar 30 2026)**;
  remote `github.com/mitchuski/mage.git` on `main` (head `00c2370` — grimoire bundle / V5.5
  attachment sync). Near-identical tree to `mymage`. Same empty `src/popup`.

### `C:\Users\mitch\swordsman-blade` — ⚔️ original published Swordsman extension
- **State:** remote `github.com/mitchuski/swordsman.git` (head `9f4b760` — Selene's Spellbook
  reflection chronicle 05-30). `dist/` build **stale (Mar 30)**. Near-identical to
  `myswordsman`.

### Cross-cutting open thread — the naming/duplication tangle (from 06-11, now *worse-shaped*)
The 06-11 consolidation chronicle flagged: should `myswordsman`/`mymage` inherit the
published history of `swordsman-blade`/`mages-spell`, or start fresh? **Decision taken =
start fresh:** mymage/mysword got brand-new repos. Net effect: **FOUR published extension
repos for TWO extensions** — `mage` + `mymage`, `swordsman` + `mysword` — with the
`-blade`/`-spell` originals now orphaned of forward work. This needs a canonical-repo
decision (archive the originals? or treat originals as published-baseline and new repos as
v2?). No extension is on the Chrome Web Store; both are `v0.1.0 · local`.

## Guide coverage (projected vs missing) — table
| guide site | item | status | source path |
|---|---|---|---|
| atlas | mysword (Swordsman · Privacy Blade) — gateway node | COVERED (lore/node) | `~/.wiki/atlas.localhost/pages/mysword-swordsman-privacy-blade` |
| atlas | mymage (Mage · Spellbook Projection) — gateway node | COVERED (lore/node) | `~/.wiki/atlas.localhost/pages/mymage-mage-spellbook-projection` |
| atlas | myterms · IEEE 7012 | COVERED | `~/.wiki/atlas.localhost/pages/myterms-ieee-7012` |
| atlas | MyTerms Alliance founding-member application | COVERED | `~/.wiki/atlas.localhost/pages/myterms-alliance-founding-member-application` |
| atlas | MyTerms Alliance kit | COVERED | `~/.wiki/atlas.localhost/pages/myterms-myterms-alliance-kit` |
| atlas | IEEE 7012 integration plan v2 | COVERED (v2 only; v3 not built) | `~/.wiki/atlas.localhost/pages/ieee-7012-integration-plan-v2` |
| atlas | Privacy-is-Value equation explainer (myterms) | COVERED | `~/.wiki/atlas.localhost/pages/privacy-is-value-equation-explainer-myterms` |
| atlas | dual-agent-architecture / dual-territory / the-familiars | COVERED | `~/.wiki/atlas.localhost/pages/dual-agent-architecture` |
| research | Paper — The Swordsman Extension (Soulbis's browser manifestation) | COVERED | `~/.wiki/research.localhost/pages/paper-the-swordsman-extension-soulbiss-browser-manifestation` |
| research | Paper — The Mage Extension (soulbae's browser manifestation) | COVERED | `~/.wiki/research.localhost/pages/paper-the-mage-extension-soulbaes-browser-manifestation` |
| research | IEEE 7012-2025 quick reference | COVERED | `~/.wiki/research.localhost/pages/reference-ieee-7012-2025-quick-reference` |
| research | Dual-agent harness framework spec v1 | COVERED | `~/.wiki/research.localhost/pages/spec-dual-agent-harness-framework-specification-v1` |
| research | ZK Swordsman blade forge (build instructions / narrative) | COVERED | `~/.wiki/research.localhost/pages/spec-zk-swordsman-blade-forge-agent-build-instructions` |
| skill | agentprivacy-swordsman-browser / mage-personas / swordsman-personas / dual-agent-harness | COVERED | `~/.wiki/skill.localhost/pages/agentprivacy-swordsman-browser` |
| tomes | artefact-3-the-dual-agent · Tome V Act 14 (City of Mages / MyTerms Alliance) | COVERED (lore) | `~/.wiki/tomes.localhost/pages/artefact-3-the-dual-agent` |
| — | **myTerms = "Agreement Layer" three-register framing (human/legal/machine) + "danger of perfect"/Gödel + the LAN trust-task rite** | **MISSING from guide** — lives only on `mitch.vision`, and *staged/unpublished* | `~/.wiki/mitch.vision.localhost/pages/myterms-the-agreement-layer`, `…/bonjour-by-hand-and-the-trust-layer-above` |
| — | **Product / install page for the two MV3 extensions** (load-unpacked / build / version / store status) | **MISSING** — atlas pages are lore nodes, not a "get the extension" surface | (none) |
| — | **Dual-agent *harness* as a runnable artefact** (`myterms/harness/` quickstart in the guide) | PARTIAL — research has the *spec*, no "run it" page | `C:\Users\mitch\myterms\harness\README.md` |
| — | **IEEE 7012 integration plan v3 (V6)** | NOT BUILT (atlas carries v2; v3 queued in repo) | `C:\Users\mitch\myterms\ieee7012_integration_plan_v2.md` (v3 pending) |

## Suggestions (prioritized) — table
| suggestion | target | why | priority H/M/L |
|---|---|---|---|
| Resolve the 4-repo / 2-extension tangle: pick canonical repos (mymage/mysword as forward), archive or baseline-tag `mage`/`swordsman`, update `myterms/README.md` "not yet public" line | mymage, myswordsman, mages-spell, swordsman-blade, myterms/README.md | Duplicate published repos cause drift + confusion; README is now factually wrong | H |
| Port the **Agreement-Layer / three-registers / Gödel / trust-task** page from `mitch.vision` into the **guide** federation (atlas or research) — it's the freshest, strongest myTerms framing and the guide has only the older `myterms-ieee-7012` node | guide federation (atlas/research) ← `~/.wiki/mitch.vision.localhost/pages/myterms-the-agreement-layer` | The canonical agreement-layer story (3 registers, "trust that certifies itself") isn't in the guide at all | H |
| Decide + execute the `mitch.vision` push of the staged trio (`myterms-the-agreement-layer`, `bonjour-by-hand…`, `welcome-visitors`) — blocked on a vision cookie in the broker | `~/.wiki/_push_myterms.js` (run `DRY=1` first) | Page has been STAGED/unpublished since 06-28; push blocked only on missing broker cookie | H |
| Build a **product/install page** for the two extensions (load-unpacked steps, manifest version, "not on Web Store yet", link to repos) | guide or atlas | Whole bundle has rich lore but zero "how do I actually run the extension" surface | M |
| Verify the extensions still build (`node build.js` in mymage + myswordsman) and refresh the stale `dist/` (Mar 30); confirm the empty `src/popup` is intentional (inlined by build) vs a gap | mymage, myswordsman | 06-11 open item "confirm copies still build" never closed; popup dir is empty | M |
| Write integration plan **v3 (V6 edition)** — retire V5.4 language, adopt `R(t)` + register head C89 | `C:\Users\mitch\myterms\ieee7012_integration_plan_v3.md` | v2 is V5.4-era; canon is V6; v3 already "queued" | M |
| Regenerate the letter-doc PDFs (A–G) against Apr 22 markdown | myterms/*.pdf | PDFs are the Feb 5 snapshot; distribution artefacts are stale | L |
| Add a "run the harness" quickstart page to the guide (research) from `harness/README.md` | research.localhost | Spec exists; the runnable rite that *operationalises* the lore has no guide on-ramp | L |

## To get rolling tomorrow (ordered next actions)
1. **Repo decision first** (unblocks everything else): declare mymage/mysword canonical;
   decide fate of `mage`/`swordsman` originals; fix the `myterms/README.md` siblings line.
2. **Push the staged myTerms trio** to `mitch.vision.ide.earth` — get the vision owner cookie
   into `~/.wiki/.creds/cookies.json`, run `DRY=1 node ~/.wiki/_push_myterms.js` then live.
3. **Port the agreement-layer page into the guide** (atlas + research) so the guide carries
   the three-registers/Gödel/trust-task framing, not just the older `myterms-ieee-7012` node
   — use the `agentprivacy-wiki-sync` skill (gates G0–G7).
4. **Smoke-build both extensions** (`node build.js`), confirm `src/popup` emptiness is by
   design, refresh `dist/`; note whether a real popup UI is still owed.
5. **Stub a product/install page** for the two MV3 extensions in the guide.
6. (Background) draft integration plan **v3 (V6)** + regenerate the A–G PDFs.

## Notes & uncertainties
- **Push status of the staged page is uncertain.** Memory says STAGED/not-published (push
  blocked on a missing vision cookie). I did not run the pusher (read-only). The pages exist
  locally at `~/.wiki/mitch.vision.localhost/pages/`; confirm live status with `DRY=1`.
- **mymage/myswordsman now have remotes** (`mitchuski/mymage.git`, `mitchuski/mysword.git`) —
  this *resolves* the 06-11 "start fresh vs inherit" open item (they started fresh) but
  *creates* the 4-repo duplication. The 06-11 chronicle's other open items
  (stray `swordsman/myterms-swordsman/` scaffold; `swordsman/`,`mage/`,`mage-x-filter/` being
  unrelated real projects) were **not** re-checked here and may still be live.
- **`src/popup/` empty in all four extension trees** — likely the popup HTML/JS is generated
  by `build.js` rather than stored in `src`, but I did not read `build.js` to confirm; flagged
  as a build-verification item, not a confirmed gap.
- The guide federation (guide/skill/tomes/research/atlas/library/grimoire) coverage of the
  dual-agent *lore* is genuinely strong; the gaps are (a) the newest agreement-layer framing,
  (b) any *product/install* surface, (c) a v6-era integration plan.
- `MYTERMS_UNIVERSE_CARD.md` is explicitly "a reference, not canon" — the conjecture register
  (`agentprivacy-docs/research/CONJECTURE_REGISTER_V6.md`) and the grimoires win on conflict.
